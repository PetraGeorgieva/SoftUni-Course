function thesmallestNumber(n1,n2,n3){
/*    let numbers = [n1,n2,n3]
    let maxSafenumber = Number.MAX_SAFE_INTEGER
 for (let i = 0; i < numbers.length; i++){
    let number = numbers[i]
    if (number < maxSafenumber ){
        maxSafenumber = number
    }
 }
  console.log(maxSafenumber);
*/
console.log(Math.min(n1,n2,n3));
}
thesmallestNumber(2,5,3);